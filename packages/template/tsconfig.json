{
  "extends": "../../tsconfig.json",
  "include": [
	"./src/**/*.ts",
	"types"
  ],
  "compilerOptions": {
	// match output dir to input dir. e.g. dist/index instead of dist/src/index
	/*"rootDir": "./src"*/
	"outDir": "./dist"
  }
}
